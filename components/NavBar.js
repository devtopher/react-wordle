import React from "react"

export default function NavBar() {
    return <div style={{
        display: "flex",
        flexDirection: "row",
        justifyContent: "center"
    }}>
        <span>
            DevTophdle
        </span>
    </div>
}